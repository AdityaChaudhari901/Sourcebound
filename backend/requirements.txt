# --- Web framework / ASGI ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation & settings ---
pydantic==2.13.4               # langchain-core 1.4.8 / langfuse 4.9.1 require >2.10
pydantic-settings==2.7.1
email-validator==2.3.0         # required by Pydantic EmailStr (auth schemas)

# --- Structured logging ---
structlog==24.4.0

# --- Auth ---
pyjwt==2.10.1                   # JWT issue/verify (HS256)
argon2-cffi==23.1.0            # password hashing

# --- Observability ---
langfuse==4.9.1

# --- Database (async ORM + driver + migrations) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]==3.2.3          # sync driver for the blocking ingestion path
alembic==1.14.0

# --- Async ingestion (Celery + Redis) ---
celery==5.4.0                   # task queue for async ingestion
redis==5.2.1                    # Celery broker + result backend client

# --- Orchestration ---
langgraph==1.2.6                # query graph (corrective-RAG control flow)
langchain-core==1.4.8           # shared LangChain primitives (peer of langgraph)

# --- Ingestion / RAG components ---
python-multipart==0.0.20        # FastAPI file uploads
langchain-text-splitters==1.1.2 # recursive + markdown-header splitters (core 1.x)
pypdf==5.1.0                    # PDF text extraction
fastembed==0.5.1                # open BGE embeddings (ONNX, no torch)
qdrant-client==1.12.1           # vector upsert + dense retrieval
openai==1.59.6                  # OpenAI-compatible client (Groq/Gemini/Ollama/OpenAI)
tavily-python==0.5.0            # web-search fallback provider
google-genai==2.9.0             # Gemini on Vertex AI (ADC; bills GCP credits)
